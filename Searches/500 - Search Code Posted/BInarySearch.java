// binarySearch.java// Assumes array is in sorted order.// Returns index of the location of the key in the array.// Retuns –1 if not found.public int binarySearch(int[] list, int max, int key) {    int mid;    int i = 0;    int j = max-1;    while (i <= j) {        mid = (i + j) / 2;        if (list[mid] > key)            j = mid - 1;        else if (list[mid] < key)            i = mid + 1;        else            return mid;    }    return -1;}